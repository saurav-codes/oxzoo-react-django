[project]
name = "oxzoo-react-django"
version = "0.1.0"
description = "ox deploy example: Django API + React SPA"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django==5.2.17",
    "gunicorn==23.0.0",
]
